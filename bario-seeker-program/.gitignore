# Claude Code local session state — never commit
.claude/

.anchor
.DS_Store
target
**/*.rs.bk
node_modules
test-ledger
.yarn

# Demo actor keypairs generated by scripts/seed-devnet.ts.
# Devnet-only and worthless, but there is no reason to publish private keys.
scripts/.demo-keys.json

# Metaplex Core, dumped from devnet for the local test validator.
# Regenerate with the command in the README.
tests/fixtures/*.so
.keys/
